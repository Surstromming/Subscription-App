import React from 'react';

import Header from './../components/Header/Header';

export default class NoMatch extends React.Component {
  render() {
    return (
      <div>
        <Header title="No page found" />
      </div>
    );
  }
}
